# Temporary source-transfer helper. Local DSR receipts remain release authority.
name: one-shot hydration source snapshot

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/fss-one-shot-hydration-reconcile.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build exact source snapshot
        shell: bash
        run: |
          set -euo pipefail
          commit_sha="$(git rev-parse HEAD)"
          tree_sha="$(git rev-parse HEAD^{tree})"
          archive="franken-surveillance-system-${commit_sha}.tar.gz"
          git archive --format=tar.gz --output="${archive}" HEAD
          archive_sha="$(sha256sum "${archive}" | awk '{print $1}')"
          printf '%s\n' \
            "repository=Dicklesworthstone/franken_surveillance_system" \
            "commit_sha=${commit_sha}" \
            "tree_sha=${tree_sha}" \
            "archive=${archive}" \
            "archive_sha256=${archive_sha}" \
            > SOURCE_SNAPSHOT.env
          sha256sum --check < <(printf '%s  %s\n' "${archive_sha}" "${archive}")
      - name: Upload exact source snapshot
        uses: actions/upload-artifact@v4
        with:
          name: franken-surveillance-system-${{ github.sha }}
          path: |
            franken-surveillance-system-${{ github.sha }}.tar.gz
            SOURCE_SNAPSHOT.env
          if-no-files-found: error
          retention-days: 1
