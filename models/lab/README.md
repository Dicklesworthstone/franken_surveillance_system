# Model qualification lab

Model code and weights are never vendored casually. Each candidate needs an immutable model
identity, license record, acquisition manifest, checksum, input/output schema, resource envelope,
held-out security evaluation, calibration report, and removal procedure before promotion.
