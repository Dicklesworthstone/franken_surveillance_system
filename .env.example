# FSS intentionally does not define vendor credentials in a tracked dotenv file.
# Adapters MUST retrieve secrets from the configured operating-system secret provider.
FSS_CONFIG_DIR=./config
FSS_DATA_DIR=./data
FSS_LOG=info
